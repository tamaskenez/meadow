name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
  workflow_dispatch:
    inputs:
      build_type:
        type: choice
        description: 'Build type'
        default: Debug
        options:
          - Debug
          - Release

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
            cmake_generator: -G Xcode
          - os: windows-latest
            cmake_generator: ""

    env:
      BUILD_TYPE: ${{ inputs.build_type || 'Debug' }}

    steps:
      - uses: actions/checkout@v6

      - name: Cache Conan packages
        id: conan-cache
        uses: actions/cache@v5
        with:
          path: id
          key: conan-${{ matrix.os }}-${{ hashFiles('conanfile.txt') }}

      - name: Install Conan
        if: steps.conan-cache.outputs.cache-hit != 'true'
        uses: conan-io/setup-conan@v1

      - name: Build Conan dependencies
        if: steps.conan-cache.outputs.cache-hit != 'true'
        shell: bash
        run: ./1_build_conan.sh --use-deployer

      - name: Configure
        run: cmake -S . -B _b ${{ matrix.cmake_generator }}

      - name: Build
        run: cmake --build _b --config ${{ env.BUILD_TYPE }}

      - name: Test
        run: ctest --test-dir _b -C ${{ env.BUILD_TYPE }} --output-on-failure
